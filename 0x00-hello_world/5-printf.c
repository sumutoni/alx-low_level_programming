#include <stdio.h>

/**
 * main - displays to standard output using printf function
 *
 * Return: 0 to signify success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
