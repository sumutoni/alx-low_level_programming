#include "main.h"

/**
 * _isupper - function checks if character is uppercase
 * @c: character to test
 *
 * Return: 0 if not upper, and 1 if upper
 */
int _isupper(int c)
{
	if (isupper(c) != 0 )
	{
		return (1)
	}
	else
	{
		return (0);
	}
}
