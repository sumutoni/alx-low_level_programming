#include <stdio.h>
#include "main.h"

/**
 * main - function to test function that prints if int is positive or negative 
 * Return: 0 signify success
 */
int main(void)
{
	int i = 0;

	positive_or_negative(i);
	return (0);
}
